<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <filter>
        <filter-name>AuthenticationFilter</filter-name>
        <filter-class>com.apu.hrms.filter.AuthenticationFilter</filter-class>
    </filter>

    <filter>
        <filter-name>RoleAuthorizationFilter</filter-name>
        <filter-class>com.apu.hrms.filter.RoleAuthorizationFilter</filter-class>
    </filter>

    <!-- Order matters: authentication first, then role checks -->
    <filter-mapping>
        <filter-name>AuthenticationFilter</filter-name>
        <url-pattern>/manager/*</url-pattern>
        <url-pattern>/counter/*</url-pattern>
        <url-pattern>/housekeeper/*</url-pattern>
        <url-pattern>/customer/*</url-pattern>
        <url-pattern>/profile</url-pattern>
        <url-pattern>/profile/*</url-pattern>
    </filter-mapping>

    <filter-mapping>
        <filter-name>RoleAuthorizationFilter</filter-name>
        <url-pattern>/manager/*</url-pattern>
        <url-pattern>/counter/*</url-pattern>
        <url-pattern>/housekeeper/*</url-pattern>
        <url-pattern>/customer/*</url-pattern>
        <url-pattern>/profile</url-pattern>
        <url-pattern>/profile/*</url-pattern>
    </filter-mapping>

</web-app>
